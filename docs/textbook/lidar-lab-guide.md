# 机械旋转式激光雷达实验指导书

## 实验概述

本实验指导书配合《机械旋转式激光雷达教学演示系统》主教材使用，通过一系列实践操作，帮助学生深入理解 LiDAR 技术原理。

---

## 实验一：环境配置与程序运行

### 实验目的

1. 掌握 Python 虚拟环境的创建与使用
2. 完成项目依赖的安装
3. 成功运行 LiDAR 演示程序

### 实验环境

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 10+ / macOS 10.14+ / Linux |
| Python 版本 | 3.8 或更高 |
| 显卡 | 支持 OpenGL 3.0+ |
| 内存 | 4GB 以上 |

### 实验步骤

#### 步骤 1：获取项目代码

```bash
# 如果使用 Git
git clone <项目仓库地址>
cd lidar_architecture

# 或直接解压项目压缩包
```

#### 步骤 2：创建虚拟环境

```bash
# 创建虚拟环境
python -m venv venv

# 激活虚拟环境
# Windows:
venv\Scripts\activate
# macOS/Linux:
source venv/bin/activate
```

#### 步骤 3：安装依赖

```bash
# 安装所有依赖
pip install -r requirements.txt

# 验证安装
python -c "import PyQt5; import OpenGL; import numpy; print('所有依赖安装成功！')"
```

#### 步骤 4：运行程序

```bash
python main.py
```

### 实验验证

程序启动后，应看到以下界面元素：

- [ ] 左侧 3D 渲染区域显示 LiDAR 模型
- [ ] 右侧控制面板显示参数滑块
- [ ] 可以用鼠标旋转、缩放视角
- [ ] 状态栏显示当前帧率

### 常见问题

| 问题 | 解决方案 |
|------|---------|
| `ModuleNotFoundError: No module named 'PyQt5'` | 确保已激活虚拟环境，重新运行 `pip install -r requirements.txt` |
| OpenGL 相关错误 | 更新显卡驱动，确保硬件支持 OpenGL 3.0+ |
| 程序闪退 | 检查 Python 版本是否为 3.8+，查看终端错误信息 |

---

## 实验二：参数调节实验

### 实验目的

1. 理解转速（RPM）对点云密度的影响
2. 掌握激光线数与垂直覆盖范围的关系
3. 观察垂直视场角（FOV）对扫描效果的影响

### 实验器材

- 已配置完成的 LiDAR 演示程序
- 实验记录表格

### 实验内容

#### 2.1 转速实验

**实验原理**：转速（RPM）决定 LiDAR 每秒旋转的圈数，影响水平方向的采样密度。

**实验步骤**：

1. 启动程序，将激光线数设为 16
2. 将垂直 FOV 设为 30°
3. 分别设置转速为 5、10、15、20 RPM
4. 观察并记录点云密度变化

**数据记录表**：

| 转速 (RPM) | 每圈水平点数（估算） | 点云密度观察 | 旋转一周时间 |
|-----------|-------------------|-------------|-------------|
| 5 | | 密集 / 适中 / 稀疏 | 秒 |
| 10 | | 密集 / 适中 / 稀疏 | 秒 |
| 15 | | 密集 / 适中 / 稀疏 | 秒 |
| 20 | | 密集 / 适中 / 稀疏 | 秒 |

**思考题**：
- 为什么转速越高，点云越稀疏？
- 在实际自动驾驶中，如何平衡转速和点云密度？

#### 2.2 激光线数实验

**实验原理**：激光线数决定垂直方向的采样密度，影响对高度变化的检测能力。

**实验步骤**：

1. 将转速固定为 10 RPM
2. 将垂直 FOV 固定为 30°
3. 分别设置激光线数为 4、8、16、32
4. 观察点云在垂直方向的分布变化

**数据记录表**：

| 激光线数 | 垂直角度间隔 | 地面检测效果 | 车辆轮廓清晰度 |
|---------|------------|-------------|---------------|
| 4 | ° | 差 / 中 / 好 | 差 / 中 / 好 |
| 8 | ° | 差 / 中 / 好 | 差 / 中 / 好 |
| 16 | ° | 差 / 中 / 好 | 差 / 中 / 好 |
| 32 | ° | 差 / 中 / 好 | 差 / 中 / 好 |

**计算公式**：
```
垂直角度间隔 = 垂直FOV / (激光线数 - 1)
```

#### 2.3 垂直视场角实验

**实验原理**：垂直 FOV 决定 LiDAR 能够扫描的垂直范围。

**实验步骤**：

1. 将转速固定为 10 RPM
2. 将激光线数固定为 16
3. 分别设置垂直 FOV 为 15°、30°、45°、60°
4. 观察地面和高处物体的检测情况

**数据记录表**：

| 垂直 FOV | 地面点云覆盖 | 车辆顶部检测 | 适用场景 |
|---------|-------------|-------------|---------|
| 15° | 差 / 中 / 好 | 差 / 中 / 好 | |
| 30° | 差 / 中 / 好 | 差 / 中 / 好 | |
| 45° | 差 / 中 / 好 | 差 / 中 / 好 | |
| 60° | 差 / 中 / 好 | 差 / 中 / 好 | |

### 实验总结

根据以上实验数据，回答以下问题：

1. 在高速公路场景中，推荐的参数组合是？为什么？
2. 在城市道路场景中，推荐的参数组合是？为什么？
3. 如果需要检测近距离行人，应该优先调整哪个参数？

---

## 实验三：代码修改实验

### 实验目的

1. 理解程序的核心代码结构
2. 掌握基本的代码修改方法
3. 实现简单的功能扩展

### 实验内容

#### 3.1 修改 LiDAR 颜色

**任务**：将 LiDAR 模型的主体颜色从深灰色改为深蓝色

**文件位置**：`opengl/scene.py`

**修改步骤**：

1. 打开 `opengl/scene.py` 文件
2. 找到 `colors` 字典定义（约第 20 行）
3. 修改 `body` 的颜色值

```python
# 原代码
self.colors = {
    'body': [0.2, 0.2, 0.25, 1.0],  # 深灰色
    ...
}

# 修改后
self.colors = {
    'body': [0.1, 0.2, 0.4, 1.0],  # 深蓝色
    ...
}
```

**验证**：重新运行程序，观察 LiDAR 模型颜色变化

#### 3.2 修改点云颜色

**任务**：将地面点云颜色从绿色改为灰色

**文件位置**：`opengl/scene.py`

**修改步骤**：

1. 找到 `_draw_point_cloud` 方法（约第 200 行）
2. 找到 `ground` 对应的颜色设置

```python
# 原代码
elif hit_type == 'ground':
    glColor4f(0.2, 0.8, 0.3, 0.9)  # 绿色

# 修改后
elif hit_type == 'ground':
    glColor4f(0.5, 0.5, 0.5, 0.9)  # 灰色
```

**验证**：重新运行程序，观察地面点云颜色变化

#### 3.3 调整 LiDAR 高度（进阶）

**任务**：将 LiDAR 的安装高度从当前位置提高 0.5 米

**提示**：
- 找到 LiDAR 模型绘制的位置
- 修改 Y 轴方向的平移量
- 注意：修改高度后，点云生成位置也需要相应调整

```python
# 在 scene.py 中找到类似代码
glTranslatef(0, 0, 0)  # 修改 Y 坐标

# 同时需要修改点云生成的起点
lidar_origin = np.array([0.35, 0.4, 0])  # 修改 Y 坐标
```

#### 3.4 添加新的环境对象（挑战）

**任务**：在场景中添加一个立方体障碍物

**参考代码**：

```python
# 在 environment.py 中添加立方体类
class Cube:
    def __init__(self, center, size):
        self.center = np.array(center)
        self.size = size

    def get_bounds(self):
        half = self.size / 2
        return (
            self.center - half,
            self.center + half
        )

    def ray_intersect(self, ray):
        # 使用 Slab 方法
        min_bound, max_bound = self.get_bounds()
        # ... 参考 Wall 类的实现

    def render(self):
        # 绘制立方体
        glColor4f(0.8, 0.3, 0.3, 0.9)  # 红色
        # ... OpenGL 绘制代码
```

### 实验报告要求

1. 记录所有修改的代码片段（修改前后对比）
2. 截图展示修改后的效果
3. 说明修改过程中遇到的问题及解决方法

---

## 实验四：点云数据分析（扩展实验）

### 实验目的

1. 理解点云数据的格式和结构
2. 学习基本的点云处理方法
3. 实现简单的数据导出功能

### 实验内容

#### 4.1 查看点云数据结构

**任务**：在程序中添加调试输出，查看点云数据

```python
# 在 scene.py 的 generate_point_cloud 方法末尾添加
print(f"点云数量: {len(self.point_cloud_data)}")
if self.point_cloud_data:
    print(f"第一个点: {self.point_cloud_data[0]}")
    print(f"数据格式: (x, y, z, hit_type)")
```

#### 4.2 统计点云分布

**任务**：统计不同类型物体的点云数量

```python
# 添加统计代码
def analyze_point_cloud(self):
    stats = {'vehicle': 0, 'wall': 0, 'ground': 0, 'unknown': 0}
    for point in self.point_cloud_data:
        hit_type = point[3] if len(point) > 3 else 'unknown'
        stats[hit_type] = stats.get(hit_type, 0) + 1
    return stats
```

#### 4.3 导出点云数据

**任务**：将点云数据导出为 CSV 文件

```python
import csv

def export_point_cloud(self, filename='point_cloud.csv'):
    with open(filename, 'w', newline='') as f:
        writer = csv.writer(f)
        writer.writerow(['x', 'y', 'z', 'type'])
        for point in self.point_cloud_data:
            writer.writerow(point)
    print(f"导出 {len(self.point_cloud_data)} 个点到 {filename}")
```

---

## 实验报告要求

### 报告结构

1. **实验概述**（10%）
   - 实验目的
   - 实验环境

2. **实验过程**（40%）
   - 每个实验的详细步骤
   - 关键截图
   - 数据记录表格

3. **实验结果分析**（30%）
   - 参数对点云质量的影响分析
   - 代码修改效果说明
   - 遇到的问题及解决方案

4. **思考与总结**（20%）
   - 实验思考题回答
   - 学习心得
   - 对 LiDAR 技术的认识

### 评分标准

| 项目 | 优秀 (90-100) | 良好 (80-89) | 中等 (70-79) | 及格 (60-69) |
|------|--------------|-------------|-------------|-------------|
| 实验完成度 | 全部完成，有创新 | 全部完成 | 大部分完成 | 基本完成 |
| 数据记录 | 完整准确 | 基本完整 | 部分完整 | 有记录 |
| 分析深度 | 深入透彻 | 较为深入 | 基本分析 | 简单描述 |
| 报告规范 | 格式规范，图文并茂 | 格式良好 | 格式基本正确 | 格式欠佳 |

### 提交要求

- 报告格式：PDF
- 文件命名：`学号_姓名_LiDAR实验报告.pdf`
- 提交截止：实验完成后一周内
- 包含内容：报告 PDF + 修改后的代码文件

---

## 附录：快捷键速查

| 快捷键 | 功能 |
|--------|------|
| `R` | 重置视角 |
| `1` | 俯视图 |
| `2` | 正视图 |
| `3` | 侧视图 |
| `4` | 透视图 |
| `5` | 等轴视图 |
| `ESC` | 退出程序 |
| 鼠标左键拖拽 | 旋转视角 |
| 鼠标滚轮 | 缩放 |

---

*本实验指导书配套项目：机械旋转式激光雷达教学演示系统*

*最后更新：2026年2月*
